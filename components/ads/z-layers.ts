/**
 * Full-screen ad overlays: above app windows (z starts ~100, grows with focus) and
 * menubar dropdown portals (~10k), but below the home shutdown veil (100000).
 */
export const AD_FULLSCREEN_Z_INDEX = 90_000;
